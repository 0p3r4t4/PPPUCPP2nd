#include "std_lib_facilities.h"

int main()
{
    int double;
    double delete;
    char _tuuru;
    bool 3esfalso;
    // pues resulta que el $ sí vale como caracter en un nombre, aunque puede
    // no ser portable y no está especificado así en el estándar ISO, sino que
    // es más bien un extensión presente en MSVC y GNUCC
    string I_want$s {"Hola"};
    cout << I_want$s << '\n';
    int uno y dos;
}
